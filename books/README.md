# Books Folder

Place your PDF files here.

Then update the `CONFIG.pdfUrl` in `assets/js/app.js` to match the filename.

Example:
```js
const CONFIG = {
    pdfUrl:    'books/pip-the-penguin.pdf',
    bookTitle: 'Pip the Penguin Goes to School',
};
```
